apiVersion: v1
kind: Service
metadata:
  name: {{ include "ks3test_helm.fullname" . }}
  labels:
    {{- include "ks3test_helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ks3test_helm.selectorLabels" . | nindent 4 }}
